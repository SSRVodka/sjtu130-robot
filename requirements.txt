requests>=2.28
PyYAML>=6.0
Flask>=2.3

# Local mock robot server  (Flask already listed above)
# Run with:  python -m museum_tour.mock_robot_server
#            python -m museum_tour.mock_robot_server 9000   # custom port

# Testing
pytest>=7.0
pytest-timeout>=2.1
